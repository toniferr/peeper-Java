package com.toniferr.peeperJava.jdk19.hilosVirtuales;

import java.util.concurrent.Executors;

public class Pruebas {

    public static void main(String[] args) {
        // Create a VirtualThreadScheduler
//        VirtualThreadScheduler scheduler = Executors.newVirtualThreadScheduler();

        // Submit tasks to be executed by virtual threads
//        for (int i = 0; i < 10; i++) {
//            scheduler.execute(() -> {
//                // Perform some task
//                System.out.println("Task running on virtual thread: " + Thread.currentThread().getName());
//            });
//        }
    }
}
